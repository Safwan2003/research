Shared setup for both people working this repo on this machine. Read this
first, then follow YOUR OWN model-specific file:

    - Qwen2-VL track  -> safwan-prompt.txt
    - LLaVA-1.5-7B track -> ammar-prompt.txt

## If you're an AI agent with a quota (Antigravity or otherwise) reading this

Everything described below is now automated in `run_master.sh` /
`run_master.bat` at the repo root, and `run_chexpert_eval.py`. If your quota
allows it, you can still help debug/extend things, but the base flow no
longer requires an agent:

    bash run_master.sh                       # from a WSL Ubuntu terminal, or
    run_master.bat                           # double-click from Windows Explorer

It does GPU check -> Python env + library install -> CheXpert download ->
interactive model selection (Qwen2-VL / LLaVA-1.5-7B, or set MODEL= to skip
the prompt) -> runs `run_chexpert_eval.py` -> appends results to
`results/experiment_log.json`. Every step is skipped if already done
(markers in `.setup_markers/`), so re-running after an interruption resumes
instead of starting over. The rest of this file explains what it's doing and
why, plus the SAS-token security note -- read that part regardless of
whether you're a human or an agent.

This repo (research: context-aligned medical VLM) already has commits up to
48cdcae on origin/main. Start by running `git pull origin main` to sync — do
not lose any local work if this clone has uncommitted changes; check
`git status` first and stash/commit anything you find before pulling.

## CURRENT FOCUS: CheXpert, not OpenI

The priority right now is CheXpert (Section 5.4 of the paper), not the OpenI
1,000-study reproduction. Both of your per-model files have a "CURRENT FOCUS"
section at the top — do that first. The OpenI-specific tasks are still in
your file but marked deferred; don't start them until told otherwise.

Read CLAUDE.md at the repo root first — it defines the project conventions
(keep torch/transformers imports local to functions, match paper terminology
like F_rad/F_xai/F_voc/F_tool, structured output schema) and lists "Known
gaps" #1-6. Also read
docs/superpowers/specs/2026-07-22-results-persistence-design.md — it
describes the append-only JSON results log (results/experiment_log.json)
via src/evaluation/results_store.py, and
src/evaluation/responsible_ai_metrics.py for Table 3/4 metrics.

## This machine

Windows host with Ubuntu via WSL2, NVIDIA GeForce RTX 5060 Ti, 16GB VRAM.

Do all the work from the Ubuntu/WSL side, not native Windows — the repo's
scripts, paths, and conventions all assume a Linux shell. WSL2 uses the
Windows NVIDIA driver directly; you do NOT need to install a separate Linux
GPU driver inside WSL. Before installing anything, confirm the GPU is
actually visible from inside WSL:

    nvidia-smi

If that fails, stop and tell the user — it usually means the Windows-side
NVIDIA driver needs updating (CUDA-on-WSL support requires a recent driver),
not something to work around from inside Linux.

16GB VRAM is comfortably enough for the 2B Qwen2-VL backbone in fp16/bf16
with room to spare, and should handle either 7B model (Qwen2-VL-7B or
LLaVA-1.5-7B) one at a time in fp16 — watch nvidia-smi for headroom, and
fall back to 8-bit (bitsandbytes) loading only if you actually hit an OOM.
Don't try to load both 7B models into VRAM simultaneously on one GPU — if
both people are working this machine at the same time, coordinate so only
one heavy model is resident at once.

Set up the environment inside WSL:

    cd <repo>
    python3 -m venv .venv && source .venv/bin/activate
    pip install -r requirements.txt   # installs both the lightweight and GPU sections
    python3 -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"

That last line must print `True` and the 5060 Ti's name before you proceed —
if it prints False, the torch build installed is CPU-only; reinstall torch
with a CUDA wheel matching your driver (see pytorch.org's install selector)
rather than working around it.

## CheXpert data + loader gap (shared infrastructure — whoever gets there first does it, the other pulls the result)

Get access: https://stanfordmlgroup.github.io/competitions/chexpert/ or
https://aimi.stanford.edu/datasets/chexpert-chest-x-rays — both require a
data-use-agreement/registration (unlike OpenI). Check with the professor
first — the lab may already have a copy on a shared drive/server, or you may
need to register yourselves. The smaller "CheXpert-small" release is fine to
start with; you don't need the full-resolution ~440GB version.

**Downloading via the AIMI Azure SAS link:** after registering, AIMI's portal
gives you a signed URL like
`https://aimistanforddatasets01.blob.core.windows.net/<container>?sv=...&sr=c&sig=...&st=...&se=...&sp=rl`.
The `sig=`/`st=`/`se=` part is a Shared Access Signature (SAS) token — a
bearer credential, same category as an API key. It expires (`se=`) and it is
scoped to whoever registered for it.

**NEVER commit this URL to git or paste it into a shared prompt file.**
Anyone holding the full URL can download the entire dataset with no login,
which would let others bypass Stanford's data-use agreement. Store it
locally instead:

    mkdir -p secrets
    echo 'PASTE_YOUR_SAS_URL_HERE' > secrets/chexpert_sas_url.txt

`secrets/` and `.env` are already in .gitignore — `git status` should show
that file as ignored, not staged. If it ever shows up in `git status` as a
tracked/staged change, stop and ask before committing anything.

`run_master.sh` does this download step automatically (installs azcopy if
missing, checks free disk space against the ~471GB full-resolution release,
and skips it entirely if already done) — you only need to create
`secrets/chexpert_sas_url.txt` yourself, then run the master script. Doing it
by hand looks like:

    SAS_URL=$(cat secrets/chexpert_sas_url.txt)
    mkdir -p data/chexpert
    azcopy copy "$SAS_URL" "data/chexpert" --recursive=true

That mirrors the whole container (images + CSVs) into `data/chexpert`,
matching config.py's `CHEXPERT_CSV_PATH` / `CHEXPERT_IMAGES_ROOT`. `data/` is
also gitignored, so the downloaded dataset itself won't get committed either
— only code and the results log should go into git from here.

**Already fixed:** `src/data/dataset.py`'s `load_chexpert_dataset()` used to
stub out `report_text=""` and `label=None`. It now builds a label-derived
pseudo-report per study (`_build_chexpert_pseudo_report`, matching the
paper's description of CheXpert as "shorter and more label-centric" than
OpenI, Section 4.1) and derives `label` from the "No Finding" column
(0=normal, 1=abnormal), with an `--uncertain-policy` choice
(`u_zeros`/`u_ones`) for how CheXpert's -1 "uncertain" values are handled --
recorded in every run's `config_snapshot` for reproducibility. You shouldn't
need to touch this again unless the actual CSV's column names/format differ
from what's implemented (check the `Path` column's path-prefix convention
against `_CHEXPERT_FINDING_COLUMNS` in dataset.py if `load_chexpert_dataset`
errors on your real CSV).

## Shared results log — both tracks write to the SAME file

    from src.evaluation.results_store import new_run_record, append_run
    import config

    run_record = new_run_record(
        mode="ablation",  # or "hallucination", "agentic_reasoning", "single", etc.
        config_snapshot={
            "seed": config.RANDOM_SEED, "n_studies": ..., "dataset": "openi",
            "model_family": "qwen2-vl",  # or "llava-1.5-7b" -- ALWAYS set this
        },
        results={"ablation": {...}},  # only the keys this run actually produced
    )
    append_run(config.RESULTS_LOG_PATH, run_record)

Always set `model_family` in config_snapshot so the two tracks' runs are
distinguishable in the same log without colliding. Never write
results/experiment_log.json by hand or truncate it — append_run already
loads-and-appends atomically.

After each meaningful real-data run: `git pull origin main` first (to pick up
anything the other person logged), then commit results/experiment_log.json
plus any code changes, and `git push origin main`. If git reports a merge
conflict inside results/experiment_log.json, resolve it by keeping BOTH
sides' new entries in the "runs" array (it's append-only by design — a
conflict there means two runs happened concurrently, not that one side is
wrong). Never resolve by discarding the other person's entries.

Sanity-check your numbers against the "Expected results to sanity-check
against" section at the bottom of CLAUDE.md — same ballpark and direction,
not exact values, since your dataset subset/seed will differ. If numbers are
wildly off in shape or direction, treat that as a bug to investigate, not
something to force-fit.
