LLaVA-1.5-7B track. Read antigravity-prompt.txt FIRST for shared environment
setup (WSL/CUDA check, install steps, results-log conventions) — this file
only covers what's specific to your model.

Unlike Qwen2-VL, there is currently NO LLaVA wrapper in this repo — only
src/vlm/reasoning.py's QwenVLReasoner exists. Your first task is to build the
LLaVA equivalent, then use it to close the same evaluation gaps Safwan is
closing for Qwen2-VL, so both columns of the paper's tables get reproduced.

Every result you log via results_store.append_run must set
`config_snapshot["model_family"] = "llava-1.5-7b"`.

## Task 0: build src/vlm/reasoning_llava.py

Create a `LlavaVLReasoner` class that mirrors QwenVLReasoner's public
interface exactly (same method names/signatures), so it's a drop-in
replacement wherever QwenVLReasoner is used:

  - `__init__(self, model_name="llava-hf/llava-1.5-7b-hf", device="cuda")`
  - `baseline_reasoning(image_path, question, report_text) -> str`
  - `context_aligned_reasoning(image_path, question, report_text, feature_card_json) -> dict`
  - `stepwise_agentic_reasoning(image_path, question, report_text, radiomics_json, feature_card_json) -> dict`
  - reuse the SAME prompt builders from src/vlm/prompts.py
    (build_baseline_prompt / build_context_aligned_prompt /
    build_stepwise_prompts) and the same `_safe_parse_json` JSON-extraction
    logic — don't duplicate that, import/reuse it.

Model loading differs from Qwen2-VL: use `transformers.LlavaForConditionalGeneration`
and `transformers.AutoProcessor` with `llava-hf/llava-1.5-7b-hf`. Follow the
repo convention of keeping `import torch` / `from transformers import ...`
local to `__init__`, not at module top-level. The chat-template and image-input
call pattern may differ slightly from Qwen2-VL's processor — check the actual
LlavaProcessor API in the installed transformers version (requirements.txt
pins transformers>=4.45.0) rather than assuming it's identical to Qwen2-VL's;
adapt `_generate()`'s internals to whatever that processor actually expects,
while keeping the public method signatures identical to QwenVLReasoner's.

Give it the same `if __name__ == "__main__":` self-test-explanation block as
reasoning.py (it can't run standalone without GPU either).

## CURRENT FOCUS: CheXpert (Known Gap #6) — do this first, before anything OpenI-specific

Read antigravity-prompt.txt's "CheXpert data + loader gap" section first —
it covers getting access, and fixing `load_chexpert_dataset()`'s two stubs
(empty report_text, null label). That part is shared infra; coordinate with
Safwan so it's not done twice.

Once CheXpert data loads correctly as `Study` objects:

1. **GradCAM classifier** — shared infra with Safwan's track, same
   coordination note as above (torchxrayvision into
   src/features/xai_gradcam.py).
2. Run your new LlavaVLReasoner (Task 0 above) on one real CheXpert
   image/report pair: baseline vs context-driven output, confirm the
   structured-JSON behavior shows up (shorter evidence, calibrated
   uncertainty, non-empty limitations/safety_note).
3. **Ablation study, BLIP text-embedding column, on CheXpert.** Note: Table 1
   compares "Qwen" vs "BLIP" text-embedding columns, NOT LLaVA — the paper's
   Figure 2 caption is explicit about this; you're contributing the BLIP
   column regardless of which VLM you use for reasoning. Extract radiomics +
   XAI + BLIP-derived text embeddings (over whatever report_text Safwan/you
   built per the loader-gap fix) for your CheXpert subset, then call
   `run_ablation()` in src/ablation/ablation_study.py. Target shape
   (Section 5.4): radiomics should beat text here — the reverse of OpenI's
   pattern (paper's OpenI "BLIP" column: text-only ~0.925, radiomics-only
   ~0.564 — expect that relationship to flip on CheXpert like it does for
   Qwen's ~0.71 vs ~0.50). That's the paper's own reported finding, not a
   bug — don't "fix" it if you see it. Log with `mode="ablation"`,
   `config_snapshot={"model_family": "llava-1.5-7b", "text_embedding_source": "blip", "dataset": "chexpert", ...}`.
4. **Hallucination + agentic reasoning on a CheXpert subset.** Use your
   `LlavaVLReasoner.stepwise_agentic_reasoning()` over a sample of CheXpert
   studies, then `hallucination.average_hallucination_rate()`,
   `responsible_ai_metrics.compute_agentic_reasoning_stats()` (step0 vs
   step1), and `compute_responsible_ai_indicators()` +
   `text_metrics.evaluate_batch()`. The paper doesn't give exact target
   numbers for CheXpert here (Tables 2/3/4 are OpenI-only) — the point is to
   see whether the same qualitative trend holds, not to hit specific values.
   Log with `config_snapshot={"model_family": "llava-1.5-7b", "dataset": "chexpert", ...}`.

## Deferred until CheXpert is done: OpenI (Known Gaps #1-5)

Don't start these yet.

- Real OpenI data — shared with Safwan's track.
- **Ablation study, BLIP text-embedding column, on ~1,000 OpenI studies**
  (target from Table 1's "BLIP" column: text-only ~0.925 AUC, radiomics-only
  ~0.564, XAI-only ~0.514, full combo ~0.930).
- **Hallucination + agentic reasoning + LLaVA-1.5-7B row of Table 4** on the
  50-study OpenI subset (target: A4_image_text is the best-performing
  variant, ROUGE-L ~0.471, BERTScore-F1 ~0.764).

Coordinate with Safwan (Qwen2-VL track, safwan-prompt.txt) before either of
you loads a 7B model onto the GPU at the same time — see the VRAM-sharing
note in antigravity-prompt.txt.
