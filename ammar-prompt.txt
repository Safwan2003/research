LLaVA-1.5-7B track. Read antigravity-prompt.txt FIRST for shared environment
setup (WSL/CUDA check, install steps, results-log conventions) — this file
only covers what's specific to your model.

A LLaVA wrapper (`LlavaVLReasoner`, see Task 0 below) already exists
alongside src/vlm/reasoning.py's QwenVLReasoner — use it to close the same
evaluation gaps Safwan is closing for Qwen2-VL, so both columns of the
paper's tables get reproduced.

Every result you log via results_store.append_run must set
`config_snapshot["model_family"] = "llava-1.5-7b"`.

## Task 0: build src/vlm/reasoning_llava.py — already done

`src/vlm/reasoning_llava.py`'s `LlavaVLReasoner` now exists, mirroring
QwenVLReasoner's public interface exactly (baseline_reasoning /
context_aligned_reasoning / stepwise_agentic_reasoning), reusing the same
prompt builders and `_safe_parse_json` logic rather than duplicating them.
It uses `transformers.LlavaForConditionalGeneration` + `AutoProcessor` with
`llava-hf/llava-1.5-7b-hf`. It has NOT been run against a real GPU yet (this
was written without one available) — if `_generate()`'s processor calls
don't match your installed transformers version's actual LlavaProcessor API,
fix that file's `_generate()` method; the public method signatures should
stay identical to QwenVLReasoner's so `run_chexpert_eval.py --model
llava-1.5-7b` keeps working as a drop-in.

## CURRENT FOCUS: CheXpert (Known Gap #6) — do this first, before anything OpenI-specific

This is now automated. From the repo root:

    MODEL=llava-1.5-7b bash run_master.sh
    # or just: bash run_master.sh   (and pick [2] LLaVA-1.5-7B at the prompt)

That handles GPU check, environment setup, the CheXpert download (once
you've put your SAS URL in secrets/chexpert_sas_url.txt — see
antigravity-prompt.txt), the GradCAM classifier (torchxrayvision, wired into
run_chexpert_eval.py's feature extraction), the ablation study, and
hallucination/agentic-reasoning/text-metrics evaluation using your
LlavaVLReasoner — logging everything to results/experiment_log.json.
`load_chexpert_dataset()`'s old stubs (empty report_text, null label) are
already fixed.

Note on Table 1: it compares "Qwen" vs "BLIP" text-embedding columns, NOT
LLaVA — `run_chexpert_eval.py` currently uses sentence-transformers for the
ablation's text-embedding feature regardless of which VLM you pick (tagged
`text_embedding_source` in the logged config_snapshot), which is an
approximation of the paper's exact Qwen/BLIP columns, not a literal
reproduction — flag this if it matters for how the professor wants results
presented. Target shape either way (Section 5.4): radiomics should beat text
on CheXpert — the reverse of OpenI's pattern. That's the paper's own
reported finding, not a bug. The paper doesn't give exact target numbers for
CheXpert's hallucination/Table 3/4-style metrics (those tables are
OpenI-only) — the point there is whether the same qualitative trend holds,
not hitting specific values.

If you want a smaller/faster run first to sanity-check the pipeline before
committing to the full 1,000/50-study run:

    MODEL=llava-1.5-7b N_ABLATION=100 N_AGENTIC=10 bash run_master.sh

## Deferred until CheXpert is done: OpenI (Known Gaps #1-5)

Don't start these yet.

- Real OpenI data — shared with Safwan's track.
- **Ablation study, BLIP text-embedding column, on ~1,000 OpenI studies**
  (target from Table 1's "BLIP" column: text-only ~0.925 AUC, radiomics-only
  ~0.564, XAI-only ~0.514, full combo ~0.930).
- **Hallucination + agentic reasoning + LLaVA-1.5-7B row of Table 4** on the
  50-study OpenI subset (target: A4_image_text is the best-performing
  variant, ROUGE-L ~0.471, BERTScore-F1 ~0.764).

Coordinate with Safwan (Qwen2-VL track, safwan-prompt.txt) before either of
you loads a 7B model onto the GPU at the same time — see the VRAM-sharing
note in antigravity-prompt.txt.
