Qwen2-VL track. Read antigravity-prompt.txt FIRST for shared environment
setup (WSL/CUDA check, install steps, results-log conventions) — this file
only covers what's specific to your model.

Your model backbone already has a working wrapper: src/vlm/reasoning.py's
QwenVLReasoner. It uses config.py's VLM_MODEL_NAME ("Qwen/Qwen2-VL-2B-Instruct"
by default) and exposes baseline_reasoning() / context_aligned_reasoning() /
stepwise_agentic_reasoning() — you should not need to change its interface,
only exercise it against real data and (for the 7B Table 4 run) point it at
"Qwen/Qwen2-VL-7B-Instruct" instead.

Every result you log via results_store.append_run must set
`config_snapshot["model_family"] = "qwen2-vl"`.

## CURRENT FOCUS: CheXpert (Known Gap #6) — do this first, before anything OpenI-specific

Read antigravity-prompt.txt's "CheXpert data + loader gap" section first —
it covers getting access, and fixing `load_chexpert_dataset()`'s two stubs
(empty report_text, null label). That part is shared infra; coordinate with
Ammar so it's not done twice.

Once CheXpert data loads correctly as `Study` objects:

1. **GradCAM classifier.** Plug a real pretrained chest X-ray classifier into
   src/features/xai_gradcam.py's GradCAM usage — use `torchxrayvision`
   (pretrained on CheXpert/NIH/MIMIC) since no lab fine-tuned DenseNet-121 is
   available yet. Wire it into run_pipeline.py's run_single().

2. **Ablation study, Qwen text-embedding column, on CheXpert.** Extract
   radiomics + XAI + Qwen-derived text embeddings (over whatever
   report_text you built per the loader-gap fix) for your CheXpert subset,
   then call `run_ablation()` in src/ablation/ablation_study.py. Target
   shape (Section 5.4): radiomics ~0.71 AUC BEATS text ~0.50 here — the
   reverse of OpenI's pattern. That's the paper's own reported finding, not
   a bug — don't "fix" it if you see it. Log with `mode="ablation"`,
   `config_snapshot={"model_family": "qwen2-vl", "dataset": "chexpert", ...}`.

3. **Hallucination + agentic reasoning on a CheXpert subset.** Same
   evaluation functions as OpenI would use: run
   `QwenVLReasoner.stepwise_agentic_reasoning()` over a sample of CheXpert
   studies, then `hallucination.average_hallucination_rate()`,
   `responsible_ai_metrics.compute_agentic_reasoning_stats()` (step0 vs
   step1), and `compute_responsible_ai_indicators()` +
   `text_metrics.evaluate_batch()`. The paper doesn't give exact target
   numbers for CheXpert here (Tables 2/3/4 are OpenI-only) — the point is to
   see whether the SAME qualitative trend holds (context alignment still
   reduces hallucination / stays calibrated), not to hit specific values.
   Log each with `config_snapshot={"model_family": "qwen2-vl", "dataset": "chexpert", ...}`.

Stage 3 single-study sanity check (`python run_pipeline.py single --image
<path> --report <path>`) works the same regardless of dataset — use a
CheXpert image/report pair for this instead of OpenI's.

## Deferred until CheXpert is done: OpenI (Known Gaps #1-5)

Don't start these yet.

- Real OpenI data (https://openi.nlm.nih.gov/faq) into data/openi/reports
  and data/openi/images per config.py's paths; validate `_parse_openi_xml()`
  in src/data/dataset.py against real XML files (AbstractText/parentImage
  tag names have version variance).
- Ablation study, Qwen column, on ~1,000 OpenI studies (target: text-only
  ~0.918 AUC, radiomics-only ~0.555, XAI-only ~0.519, full combo ~0.925).
- Hallucination + agentic reasoning on the 50-study OpenI subset, reproducing
  Tables 2/3/4's actual target numbers (HR 1.14->0.25, uncertainty
  ~0.70->~0.68, evidence length ~19.4->~15.3 words, plus the Qwen2-VL-7B row
  of Table 4 using "Qwen/Qwen2-VL-7B-Instruct").

Coordinate with Ammar (LLaVA track, ammar-prompt.txt) before either of you
loads a 7B model onto the GPU at the same time — see the VRAM-sharing note
in antigravity-prompt.txt.
